@import styleguide.gspec

@objects
    header              id      header
    menu                id      menu

= Navigation =
    header:
        height 70px
        width 100% of viewport/width
    menu:
        @on desktop, tablet
            height ~ 63px
        @on mobile
            height ~ 126px
        width 100% of viewport/width
