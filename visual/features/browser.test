@@ Set
    domain  galenframework.com
    size    1200x768
    gridUrl http://localhost:4444/wd/hub

@@ Table browsers
    | browser            | browserName        |
    | firefox            | Firefox            |
    | chrome             | Chrome             |


@@ Parameterized using browsers
Home page in ${browserName} browser
  selenium grid ${gridUrl} --browser "${browser}" --page "http://${domain}" --size ${size}
      check qa/visual/features/specs/homepage.spec
